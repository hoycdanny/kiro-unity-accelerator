{
  "id": "wf-test-execution",
  "name": "Test Execution",
  "description": "Runs EditMode tests, then PlayMode tests, and aggregates the results into a structured report.",
  "isBuiltIn": true,
  "steps": [
    {
      "id": "run-editmode",
      "name": "Start EditMode tests (returns job_id; poll separately with get_test_job)",
      "mcpTool": "run_tests",
      "mcpAction": "n/a",
      "mcpArgs": { "mode": "EditMode" },
      "dependsOn": [],
      "onFailure": "pause"
    },
    {
      "id": "run-playmode",
      "name": "Start PlayMode tests (returns job_id; poll separately with get_test_job)",
      "mcpTool": "run_tests",
      "mcpAction": "n/a",
      "mcpArgs": { "mode": "PlayMode" },
      "dependsOn": [],
      "onFailure": "pause"
    },
    {
      "id": "aggregate-results",
      "name": "Aggregate test results from console output",
      "mcpTool": "read_console",
      "mcpAction": "get",
      "mcpArgs": { "filter_text": "TestRunner" },
      "dependsOn": ["run-editmode", "run-playmode"],
      "onFailure": "skip"
    }
  ],
  "createdAt": "2026-01-01T00:00:00Z"
}
